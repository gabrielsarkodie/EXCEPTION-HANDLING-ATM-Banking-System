# EXCEPTION-HANDLING-ATM-Banking-System

This repository contains an object-oriented Python implementation of a banking ATM system. It includes a `BankAccount` class supporting deposits, withdrawals, and balance checks, alongside custom exception handling for negative deposits, invalid withdrawal amounts, and insufficient funds.
